import { FlagProtocolAgent } from "@/components/FlagProtocolAgent";

export default function Page() {
  return (
    <main className="min-h-screen bg-gradient-to-b from-sky-100/60 via-white to-slate-100">
      <FlagProtocolAgent />
    </main>
  );
}
